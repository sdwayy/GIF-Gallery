export type AppConfigType = {
  KEY_WORD_FOR_AUTOUPDATE: string,
  AUTOUPDATE_DELAY_MS: number,
  API_KEY: string,
};

export type ImageType = {
  tag: string
  url: string
  associatedId?: number
};
